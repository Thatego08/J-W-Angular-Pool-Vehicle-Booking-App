name: Build and deploy Angular app to Azure Web App - team34Angular

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - uses: actions/checkout@v4

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      # Install dependencies
      - name: Install dependencies
        run: npm install

      # Build Angular app
      - name: Build Angular
        run: npm run build -- --output-path=dist/admin-account/browser --configuration=production

      # Upload the browser build for deployment
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: angular-dist
          path: dist/admin-account/browser

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # Download the artifact
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: angular-dist

      # Deploy to Azure Web App
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'team34Angular'
          slot-name: 'Production'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
          package: 'dist/admin-account/browser'
